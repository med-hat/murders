We analyze US gun murder data

download-data.R - downloads a csv file to data directory

wrangle-data.R - creates a wrangles dataset and saves it as R object in rda directory

analysis.R - generates a plot and saves it to figs directory.